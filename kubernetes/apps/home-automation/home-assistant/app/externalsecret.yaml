---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: home-assistant
  namespace: default
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: home-assistant-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        HASS_TOKEN: "{{ .HASS_TOKEN }}"
        HASS_POSTGRES_URL: "postgresql://{{ .POSTGRES_USER }}:{{ .POSTGRES_PASS }}@postgres16-rw.database.svc.cluster.local/home-assistant"
        LATITUDE: "{{ .LATITUDE }}"
        LONGITUDE: "{{ .LONGITUDE }}"
        # ALARMCD: "{{ .ALARMCD }}" /
        # SECURE_DEVICES_PIN: "{{ .SECURE_DEVICES_PIN }}"
        # OPNSENSE_USER: "{{ .OPNSENSE_USER }}"
        # OPNSENSE_KEY: "{{ .OPNSENSE_KEY }}"
        # DOORBELL_PASSWORD: "{{ .DOORBELL_PASSWORD }}"
        # Postgres Init
        POSTGRES_DB: home-assistant
        POSTGRES_HOST: postgres16-rw.database.svc.cluster.local
        POSTGRES_SUPER_PASS: "{{ .POSTGRES_SUPER_PASS }}"
        POSTGRES_PASS: "{{ .POSTGRES_PASS }}"
        POSTGRES_USER: "{{ .POSTGRES_USER }}"
  dataFrom:
  - extract:
      key: home-assistant
  - extract:
      key: cloudnative-pg
